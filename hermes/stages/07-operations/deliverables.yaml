stage: 07-operations
name: Operations（监控运维）

deliverables:
  - id: runbook
    type: file
    path: runbook.md
    required: true
    min_size_bytes: 500
    must_contain:
      - "故障处理"

  - id: monitoring-doc
    type: file
    path: monitoring.md
    required: true
    min_size_bytes: 500
    must_contain:
      - "告警"
